{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm install --prefix ../..",
  "buildCommand": "cd ../.. && npm run build -w packages/shared && cd packages/api && npm run build",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
